spring:
  datasource:
    url: jdbc:h2:mem:useraddress
    driverClassName: org.h2.Driver
    username: admin
    password: admin
  jpd:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /console